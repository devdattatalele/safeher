import { Text, View } from 'react-native';

export default function Entry() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Default Entry Point</Text>
    </View>
  );
}